<table>
<tr>
	<th>ID</th>
	<th>Nombre</th>
	<th>Imagen</th>
	
</tr>

<% @protectors.each do |p| %>
<tr>
	<td><%=p.id%></td>
	<td><%=p.nombre%></td>
	<td><%=p.imagen%></td>
	<td><%=link_to 'Eliminar',protector_path(p), method: :delete%></td>
	<td><%= link_to 'Editar', edit_protector_path(p)%></td>
</tr>

<% end %>
</table>
<%= link_to 'Crear protector', new_protector_path%>